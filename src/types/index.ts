export interface DemoItem {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
  demoUrl: string;
  icon: string;
}

export interface ServiceItem {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface FormData {
  name: string;
  email: string;
  company: string;
  message: string;
}